| Method | URL               | Description  |
| ------ | ----------------- | ------------ |
| GET    | `/`               | Home API     |
| POST   | `/api/auth/login` | Login        |
| GET    | `/api/products`   | Get Products |
| POST   | `/api/products`   | Add Product  |


{
    "name":"iPhone 16",
    "price":89999,
    "category":"Mobile",
    "image":"iphone.jpg"
}


npm install
npm run dev


backend/
│
├── tests/
│   ├── product.test.js
│   ├── auth.test.js
│   └── product.service.test.js
│
└── ...


Product API — product.test.js

✓ GET /api/products returns products
✓ GET /api/products returns 200
✓ POST /api/products creates a product
✓ POST /api/products rejects unauthenticated request
✓ POST /api/products rejects invalid product data


Authentication — auth.test.js


✓ POST /api/auth/login returns JWT
✓ POST /api/auth/login returns error for invalid credentials
✓ Protected API rejects missing token


Service layer — product.service.test.js


✓ Service returns all products
 Service creates a product



Backend
│
├── API Tests
│   ├── GET products
│   ├── POST products
│   ├── Authentication
│   └── Validation
│
├── Auth Tests
│   ├── Successful login
│   └── Failed login
│
└── Service Tests
    ├── Get products
    └── Create product


                    Mini Amazon
                         │
          ┌──────────────┼──────────────┐
          │              │              │
       Backend         React           Vue
          │              │              │
       Jest +          Vitest +       Vitest +
       Supertest       RTL            Vue Test Utils
          │              │              │
          ▼              ▼              ▼
       REST APIs      Components     Components
          │
          ▼
       Services
          │
     ┌────┴────┐
     ▼         ▼
  MongoDB    MySQL


  Full Stack App
      ↓
Backend Tests
      ↓
React Tests
      ↓
Vue Tests
      ↓
Docker
      ↓
CI/CD
      ↓
GitHub Portfolio Project


product.test.js
        ↓
REST API tests

auth.test.js
        ↓
Authentication tests

product.service.test.js
        ↓
Service layer tests


Testing Tools

We'll use:

Jest
Supertest
Jest

Used to run tests and make assertions.

Example:

expect(result.statusCode).toBe(200);
Supertest

Used to test Express REST APIs.

Example:

request(app)
    .get("/api/products");


    
Why?

We don't want Jest to start the server.

Instead:

Test
 ↓
app.js
 ↓
Express
 ↓
Routes

Therefore:

const app = require("../app");


What These Tests Do

We now test:

Test 1
GET /api/products

Expected:

200 OK
Test 2

Checks that the response is an array:


Test 2

Checks that the response is an array:

expect(Array.isArray(response.body))
    .toBe(true);
Test 3

Try creating a product without JWT:

POST /api/products
       ↓
No Authorization
       ↓
401 Unauthorized
Test 4

Create a product using JWT:

POST /api/products
       ↓
JWT
       ↓
Product created
Test 5

Send invalid data:

price: -100
name: ""

Expected:

400 Bad Request


. What Authentication Tests Verify

We're checking:

Correct login
      ↓
JWT returned

and:

Wrong login
      ↓
Error

and:

Protected API
      ↓
No JWT
      ↓
401

This is particularly useful in a portfolio because you're demonstrating that you understand authentication testing, not just CRUD testing.


4. Final Backend Tests

You now have:

backend/tests/
│
├── product.test.js
│
├── auth.test.js
│
└── product.service.test.js


Total tests
product.test.js
├── GET products → 200
├── GET products → array
├── POST without JWT → 401
├── POST with JWT → success
└── POST invalid data → error

auth.test.js
├── Login → JWT
├── Invalid login → error
└── Protected API → 401

product.service.test.js
├── getProducts()
└── createProduct()

That's 10 backend tests.










