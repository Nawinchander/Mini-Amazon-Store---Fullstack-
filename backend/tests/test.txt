| Method | URL               | Description  |
| ------ | ----------------- | ------------ |
| GET    | `/`               | Home API     |
| POST   | `/api/auth/login` | Login        |
| GET    | `/api/products`   | Get Products |
| POST   | `/api/products`   | Add Product  |


{
    "name":"iPhone 16",
    "price":89999,
    "category":"Mobile",
    "image":"iphone.jpg"
}


npm install
npm run dev


backend/
│
├── tests/
│   ├── product.test.js
│   ├── auth.test.js
│   └── product.service.test.js
│
└── ...


Product API — product.test.js

✓ GET /api/products returns products
✓ GET /api/products returns 200
✓ POST /api/products creates a product
✓ POST /api/products rejects unauthenticated request
✓ POST /api/products rejects invalid product data


Authentication — auth.test.js


✓ POST /api/auth/login returns JWT
✓ POST /api/auth/login returns error for invalid credentials
✓ Protected API rejects missing token


Service layer — product.service.test.js


✓ Service returns all products
 Service creates a product



Backend
│
├── API Tests
│   ├── GET products
│   ├── POST products
│   ├── Authentication
│   └── Validation
│
├── Auth Tests
│   ├── Successful login
│   └── Failed login
│
└── Service Tests
    ├── Get products
    └── Create product


                    Mini Amazon
                         │
          ┌──────────────┼──────────────┐
          │              │              │
       Backend         React           Vue
          │              │              │
       Jest +          Vitest +       Vitest +
       Supertest       RTL            Vue Test Utils
          │              │              │
          ▼              ▼              ▼
       REST APIs      Components     Components
          │
          ▼
       Services
          │
     ┌────┴────┐
     ▼         ▼
  MongoDB    MySQL


  Full Stack App
      ↓
Backend Tests
      ↓
React Tests
      ↓
Vue Tests
      ↓
Docker
      ↓
CI/CD
      ↓
GitHub Portfolio Project


product.test.js
        ↓
REST API tests

auth.test.js
        ↓
Authentication tests

product.service.test.js
        ↓
Service layer tests


Testing Tools

We'll use:

Jest
Supertest
Jest

Used to run tests and make assertions.

Example:

expect(result.statusCode).toBe(200);
Supertest

Used to test Express REST APIs.

Example:

request(app)
    .get("/api/products");


    






